# R7.3 progress snapshot — 2026-08-10 20:30Z

`READY FOR TABLES = NO`.

Frozen gates remain unchanged: Advantage NRMSE <= 0.75, AveragePolicy weighted TV <= 0.12, cross-seed mean TV <= 0.15, cross-seed p95 TV <= 0.35.

## Completed in this block

### Corrected behavior-aware Advantage objective V2

Workflow `31427105766` repaired the V1 readiness bug and produced valid neural-behavior CFR evidence.

- baseline MSE: mean/p95 `0.294431 / 0.802882`, fit PASS;
- MSE + policy auxiliary 0.10: `0.235531 / 0.771797`, but Advantage NRMSE fails on both seeds (`0.892394`, `0.836072`).

Branch closed as primary solution.

### Authoritative paired partial-exact level-2 baseline

Paired workflow `31427702314` size 1 establishes the current 256-root baseline under the generation-2 deck formula and recovered single live RNG stream:

- mean `0.2456560284`
- p95 `0.6287055612`
- fit gates PASS.

Independent final-policy-ensemble workflow reproduced the exact same size-1 cross-seed metrics, providing a useful deterministic semantic cross-check.

Raw Advantage ensemble size 2:

- mean `0.2153794914` = `0.8768x` baseline;
- p95 `0.6321426630` = `1.0055x` baseline;
- fit gates PASS.

Thus size 2 improves the distribution center but not the tail. Size 4 remains physically active.

### Final AveragePolicy ensemble

Workflow `31427741160`, evidence commit `70be0290fff93e2f38030ce9010fb7b544e8137e`:

| size | mean TV | p95 TV |
|---:|---:|---:|
| 1 | 0.245656 | 0.628706 |
| 2 | 0.226901 | 0.598557 |
| 4 | 0.212912 | 0.599173 |

All fit gates pass. Size 4 improves mean by ~13.3% but p95 by only ~4.7%. Final-policy approximation is therefore material but not the dominant p95 source; no standalone promotion.

### Legal-action common-mode centering

Workflow `31428588011` tested raw, legal-mean, legal-median and legal-midrange centering on eight independently fitted AdvantageNets over identical frozen memory.

- raw size-1 mean/p95: `0.231120 / 0.803678`;
- best centering (`legal_midrange`) mean/p95: `0.219685 / 0.824299`;
- ratios: mean `0.9505`, p95 `1.0257`.

Diagnosis: `ADVANTAGE_COMMON_MODE_CENTERING_NOT_MATERIAL`. The common-offset hypothesis is closed; it slightly improves mean while worsening the tail.

## Active physical work

1. `31427702314` — authoritative paired raw Advantage ensemble size 4; physical candidate running.
2. `31428299914` — authoritative paired partial-exact + size-4 Advantage **policy mixture**; physical candidate running. This is the key nonlinear-tail candidate because same-memory mapping evidence reduced p95 from `0.472424` (average raw Advantage then RM) to `0.360312` (RM each member then average policy).
3. `31429040423` — authoritative partial-exact strategy-target support/tail forensic; physical run active. It measures shared-target disagreement by street, legal-action count and history length.
4. `31429334213` — direct regret-policy surrogate same-memory screen; smoke PASS and physical screen active. This is diagnostic only and explicitly not assumed theoretically equivalent to Deep CFR.

## Current decision rule

- Do not escalate raw Advantage ensemble merely because mean improves; p95 must improve materially too.
- If paired policy mixture materially reduces both mean and p95 with fit gates PASS, test the smallest useful mapping over more CFR iterations before 640 acceptance escalation.
- Use the partial-exact support forensic to decide whether remaining p95 belongs to strategy-target divergence or approximation/generalization.
- Direct behavior surrogate advances only if the same-memory stability gain is very large; otherwise close it without end-to-end semantic change.
- No estimator, ensemble, policy mapping or objective changes production semantics without explicit versioning and checkpoint/resume recertification.
