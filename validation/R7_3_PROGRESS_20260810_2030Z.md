# R7.3 progress snapshot — 2026-08-10

`READY FOR TABLES = NO`.

Frozen gates remain unchanged: Advantage NRMSE <= 0.75, AveragePolicy weighted TV <= 0.12, cross-seed mean TV <= 0.15, cross-seed p95 TV <= 0.35.

## Current best conservative candidate

Authoritative paired partial-exact level-2 baseline, 256 roots/seed:

```text
mean = 0.2456560284
p95  = 0.6287055612
```

Size-4 raw Advantage averaging improved mean but not p95 (`0.210869 / 0.627987`). Size-4 **policy mixture** instead regret-matches each independently fitted AdvantageNet before averaging legal-action probabilities.

Workflow `31428299914`, evidence `76ac23287961cc3c650a1b60891648bfe975b145`:

```text
mean = 0.1719404310
p50  = 0.1451251805
p95  = 0.4136051536
max  = 0.7699102759
```

Relative to baseline this is ~30.0% lower mean and ~34.2% lower p95. Both per-seed frozen fit gates pass. It is the first generation-2 conservative candidate to move both center and tail strongly, but still fails the cross-seed gates.

Workflow `31432403037` is physically running the five-iteration compounding screen: 5 × 64 = 320 roots/seed. Smoke PASS. No 640 escalation until this longer-horizon result exists.

## Causal transition physically confirmed

Authoritative partial-exact support-by-iteration workflow `31431939967` completed successfully. Evidence commit `086df6ed397ecc4a2e61728aa3b40f0f58593675`.

Iteration 1, before any fitted AdvantageNet can alter behavior:

```text
poker-isomorphic Jaccard      = 0.0125865
shared-target weighted TV     = 0.0000000
shared-target p95 TV          = 0.0000000
```

Iteration 2, after the first Advantage fit/regret-map feedback:

```text
poker-isomorphic Jaccard      = 0.0284171
shared-target weighted TV     = 0.4739458
shared-target p95 TV          = 1.0000000
```

Diagnosis: `TARGET_DIVERGENCE_APPEARS_AFTER_FIRST_ADVANTAGE_FIT`.

This confirms the leading causal chain:

```text
Advantage approximation
  -> regret mapping
  -> changed sampled behavior
  -> changed CFR trajectories
  -> divergent next Strategy Memory targets
```

The final AveragePolicy is downstream of the divergence and is not the primary p95 source.

## Direct behavior surrogate E2E completed

Same-memory screening had shown a large stability gain for a direct policy surrogate. The authoritative E2E causal screen has now completed: workflow `31431672631`, evidence commit `1d1ac9b23abd2afedec8390e3f8bb482c11b0625`.

```text
baseline partial-exact:
mean = 0.245656
p95  = 0.628706

Direct Behavior E2E:
mean = 0.142553   PASS <= 0.15
p50  = 0.100895
p95  = 0.426860   FAIL > 0.35
max  = 0.870134
```

The ordinary frozen Advantage and final AveragePolicy fits pass. However the behavior surrogate itself does not fit the sample-level regret-matched target within the reference `0.12` TV threshold: for seed `20260829`, iteration-1/iteration-2 surrogate audits remain about `0.2723 / 0.2205` at the 4096-step ceiling.

Therefore the E2E gain is best interpreted as evidence that **smoothing/regularizing the first behavior-feedback transition is powerful**, not as evidence that this surrogate is a faithful recovered Deep-CFR implementation. Because regret matching is nonlinear, theoretical equivalence is not claimed and this branch is not promotable as-is.

## Other completed exclusions

- final AveragePolicy ensemble size 4: `0.212912 / 0.599173`; approximation is secondary, tail remains large;
- legal-action common-mode centering: best mean slightly better but p95 worse; closed;
- behavior-aware Advantage objective V2: candidate Advantage fit failed; closed;
- root scaling, common-path RNG, antithetic sampling, high path replication, exhaustive opponent expectation, duplicate-target aggregation, multistart selection and card/suit rewrite all failed to explain or remove the dominant tail.

## Active physical work

`31432403037` — five-iteration size-4 policy-mixture compounding, 320 roots/seed. Build/regression/smoke PASS; physical run active.

## Decision rule

- If the five-iteration policy mixture retains a large center-and-tail reduction with frozen fit gates PASS, launch the 640 acceptance-scale candidate under exactly the same deal/RNG contract.
- If its gain collapses as feedback depth increases, do not brute-force more roots. Use the direct-behavior result only as a causal clue and test an explicit, auditable behavior-smoothing/interpolation mechanism at the first Advantage-fit transition.
- The direct behavior surrogate does not advance to production semantics while it both misses the p95 gate and underfits its own reference behavior target.
- No gate relaxation, no silent estimator/RNG/policy semantic change, and no production promotion without checkpoint/resume recertification.
