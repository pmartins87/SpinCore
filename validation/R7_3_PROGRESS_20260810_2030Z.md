# R7.3 progress snapshot — 2026-08-10

`READY FOR TABLES = NO`.

Frozen gates remain unchanged: Advantage NRMSE <= 0.75, AveragePolicy weighted TV <= 0.12, cross-seed mean TV <= 0.15, cross-seed p95 TV <= 0.35.

## Completed in this block

### Corrected behavior-aware Advantage objective V2

Workflow `31427105766` repaired the V1 readiness bug and produced valid neural-behavior CFR evidence.

- baseline MSE: mean/p95 `0.294431 / 0.802882`, fit PASS;
- MSE + policy auxiliary 0.10: `0.235531 / 0.771797`, but Advantage NRMSE fails on both seeds (`0.892394`, `0.836072`).

Branch closed as primary solution.

### Authoritative paired partial-exact level-2 control + raw Advantage ensemble

Workflow `31427702314` under the generation-2 deck formula and recovered single live RNG stream:

| raw Advantage ensemble | mean TV | p95 TV | fit gates |
|---:|---:|---:|---|
| 1 | `0.245656` | `0.628706` | PASS |
| 2 | `0.215379` | `0.632143` | PASS |
| 4 | `0.210869` | `0.627987` | PASS |

Size 4 evidence commit: `59f83c73da755f40f6590a2a24785fed9e5b48bd`.

Raw model averaging improves mean ~14% but leaves p95 effectively unchanged. This closes raw Advantage ensembling as a standalone primary solution.

### Paired size-4 Advantage policy mixture — material result

Workflow `31428299914`, evidence commit `76ac23287961cc3c650a1b60891648bfe975b145` preserves the authoritative deck schedule and primary coupled RNG stream. Each independently fitted Advantage member is hard-regret-matched first; the resulting legal-action policies are then averaged.

```text
paired size-1 baseline:
mean = 0.2456560284
p95  = 0.6287055612

size-4 policy mixture:
mean = 0.1719404310
p50  = 0.1451251805
p95  = 0.4136051536
max  = 0.7699102759
```

This is about a 30.0% reduction in mean and 34.2% reduction in p95. Both per-seed fit gates pass. Final ensemble Advantage NRMSE is `0.501434 / 0.542430`; final AveragePolicy weighted TV is `0.096840 / 0.094627`.

The candidate still fails the frozen cross-seed gates (`0.17194 > 0.15`, `0.41361 > 0.35`), but it is the first paired generation-2 mechanism to move **both** center and tail strongly under the authoritative contract.

It has therefore advanced to a five-iteration compounding screen, workflow `31432403037`: 5 × 64 = 320 roots/seed. Smoke PASS; physical run active. This is intentionally before any 640 escalation because the central question is whether the tail reduction survives repeated CFR feedback.

### Final AveragePolicy ensemble

Workflow `31427741160`, evidence commit `70be0290fff93e2f38030ce9010fb7b544e8137e`:

| size | mean TV | p95 TV |
|---:|---:|---:|
| 1 | `0.245656` | `0.628706` |
| 2 | `0.226901` | `0.598557` |
| 4 | `0.212912` | `0.599173` |

All fit gates pass. Size 4 improves mean by ~13.3% but p95 by only ~4.7%. Final-policy approximation is material but not the dominant p95 source; no standalone promotion.

### Partial-exact strategy-target/support forensic

Workflow `31429040423`, evidence `4e1ce678e93b68c598b4762a70cca0a08b05dfc2`.

Raw exact-key overlap:

- Jaccard `0.0046825`;
- mean LCFR-weight coverage `0.0094187`;
- shared-target weighted mean TV `0.329293`;
- shared-target p95 `0.750000`.

Poker-isomorphic comparison:

- Jaccard `0.0208888`;
- mean LCFR-weight coverage `0.0785715`;
- shared-target weighted mean TV `0.349296`;
- shared-target p95 `0.803535`.

The two seeds therefore share little Strategy Memory support, and the strategy targets themselves already disagree strongly on the small shared subset. The p95 problem is demonstrably upstream of final AveragePolicy approximation.

A prior controlled iteration-split screen independently showed the key transition: iteration 1, before any fitted Advantage behavior, has exactly zero shared-target disagreement; iteration 2 jumps to poker-isomorphic weighted mean TV `0.527757` and p95 `1.0`. The active authoritative replication is workflow `31431939967`.

### Legal-action common-mode centering

Workflow `31428588011`:

- raw size-1 mean/p95: `0.231120 / 0.803678`;
- best centering (`legal_midrange`) mean/p95: `0.219685 / 0.824299`.

Diagnosis `ADVANTAGE_COMMON_MODE_CENTERING_NOT_MATERIAL`: mean improves slightly while p95 worsens.

### Direct behavior surrogate — same frozen memory

Workflow `31429334213`, evidence `7095b5180b7470f76f03118fe3d5ce75760238a8`:

```text
AdvantageNet -> hard regret matching:
mean = 0.230515
p95  = 0.818211

Direct policy surrogate:
mean = 0.110418
p95  = 0.363998
```

Ratios are `0.4790` for mean and `0.4449` for p95. Diagnosis: `DIRECT_BEHAVIOR_SURROGATE_MATERIAL_SAME_MEMORY`.

This does not establish theoretical equivalence to Deep CFR. Workflow `31431672631` has passed regression and smoke and is running the physical authoritative partial-exact 256-root E2E causal screen with surrogate training isolated from the primary RNG.

## Active physical work

1. `31432403037` — five-iteration size-4 policy-mixture compounding, 320 roots/seed; smoke PASS, physical run active.
2. `31431672631` — direct behavior surrogate E2E; regression + smoke PASS, physical 256-root run active.
3. `31431939967` — authoritative partial-exact support forensic split by CFR iteration; smoke PASS, physical 256-root run active.

## Current decision rule

- If five-iteration policy mixture retains a large reduction in both mean and p95 with fit gates PASS, it earns the next 640 acceptance-scale test; do not assume the two-iteration gain survives compounding.
- If direct behavior E2E produces a very large tail reduction, stop before production promotion and formalize/version its regret semantics, RNG/checkpoint contract and deterministic resume behavior.
- If the authoritative iteration split confirms zero target disagreement at iteration 1 and large disagreement at iteration 2, the causal chain is pinned to first Advantage fit/regret-map -> altered trajectories -> divergent strategy targets. The next solution must stabilize that transition rather than smooth the final AveragePolicy.
- No estimator, ensemble, policy mapping or objective changes production semantics without explicit versioning and checkpoint/resume recertification.
