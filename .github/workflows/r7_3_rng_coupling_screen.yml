name: SpinCore R7.3 RNG coupling screen

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/r7_3_rng_coupling_screen.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: spincore-r7-3-rng-coupling-screen
  cancel-in-progress: false

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout exact main
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install pytest

      - name: Build and regress authoritative solver
        shell: bash
        env:
          PYTHONPATH: ${{ github.workspace }}/python
        run: |
          set -euo pipefail
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j2
          ctest --test-dir build --output-on-failure
          pytest -q python_tests

      - name: Smoke RNG coupling diagnostic
        shell: bash
        env:
          PYTHONPATH: ${{ github.workspace }}/python
        run: |
          set -euo pipefail
          python tools/run_r7_3_rng_coupling_screen.py \
            --solver build/libspincore_solver_c.so \
            --out validation/R7_3_RNG_COUPLING_SMOKE.json \
            --iterations 2 \
            --roots-per-iteration 2 \
            --advantage-chunk-steps 4 \
            --advantage-max-steps-per-iteration 4 \
            --advantage-fit-target 10 \
            --batch-size 16 \
            --audit-size 16 \
            --reservoir-capacity 5000
          rm -f validation/R7_3_RNG_COUPLING_SMOKE.json

      - name: Run physical 256-root RNG coupling screen
        shell: bash
        env:
          PYTHONPATH: ${{ github.workspace }}/python
        run: |
          set -euo pipefail
          python tools/run_r7_3_rng_coupling_screen.py \
            --solver build/libspincore_solver_c.so \
            --out validation/R7_3_RNG_COUPLING_SCREEN_256.json \
            --iterations 2 \
            --roots-per-iteration 128 \
            --advantage-chunk-steps 256 \
            --advantage-max-steps-per-iteration 1024 \
            --advantage-fit-target 0.70 \
            --batch-size 256 \
            --audit-size 512 \
            --reservoir-capacity 100000

      - name: Persist RNG coupling evidence
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [ ! -f validation/R7_3_RNG_COUPLING_SCREEN_256.json ]; then
            echo '{"schema":"SPINCORE_R7_3_RNG_COUPLING_SCREEN_V1","runner_failed_before_report":true,"ready_for_tables":false}' > validation/R7_3_RNG_COUPLING_SCREEN_256.json
          fi
          git config user.name 'SpinCore R7 Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add -f validation/R7_3_RNG_COUPLING_SCREEN_256.json
          if ! git diff --cached --quiet; then
            git commit -m 'Record physical R7.3 RNG coupling screen [r7-evidence-skip]'
            git pull --rebase origin main
            git push origin HEAD:main
          fi
