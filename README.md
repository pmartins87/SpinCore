# SpinCore Repository Seed v1.9 — recovered baseline

This repository is the persistent home of the SpinCore project after the previous project chat reached its file/context limit.

Recovered physical baseline:
- Git commit: `445fb4a56da69f9a3ca48acbe13a6cfb95055ac7`
- tag: `internal-r5-pass-reintegrated`
- VERSION: `1.9.0`
- status: `R5_PASS`

Historical certified state from the prior project conversation is tracked separately in `RECOVERY_STATUS.md`. Historical certification never overrides physical evidence and no frozen gate may be relaxed during recovery.

Core permanent decisions from the recovered repository:
1. True heads-up and 3-handed reduced to two active players are different strategic domains.
2. The global blueprint has a compact action abstraction with at most five actions per node.
3. `READY FOR TABLES = NO` until the finite roadmap R0–R12 passes.

Current recovery goal: re-materialize the post-R5 R6/R7 stack, validate it, then continue the R7 multi-seed stability gate exactly where the prior project stopped.
