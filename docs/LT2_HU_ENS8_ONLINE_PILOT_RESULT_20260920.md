# SpinCore — LT2 HU ENS8 online pilot 8000→8100 result

Date: 2026-09-20  
Status: **MECHANICAL/ONLINE EXECUTION PASS — STRATEGIC FORENSIC ADJUDICATION REQUIRED BEFORE HOLDOUT**

## Pilot contract completed

The isolated ENS8 pilot completed exactly 100 additional iterations:

- source: preserved iteration 8000;
- target/completed: iteration 8100;
- new roots: 60,000;
- workers: 31;
- parent Torch threads: 8;
- K4 off;
- holdout untouched;
- source checkpoint read-only.

TRUE_HEADS_UP used the predeclared ENS8_A composition:

- 8 independent fresh fits per iteration;
- 400 steps per member;
- same eight predeclared init/batch seed pairs every iteration;
- raw Advantage outputs averaged before unchanged lean regret matching;
- policy RNG isolated from ensemble minibatch RNG.

THREE_HANDED remained the canonical single fresh100 path.

## Final training state

Iteration 8100:

THREE_HANDED:
- roots: `2,648,700`;
- Advantage seen: `82,508,117`;
- strategy seen: `5,998,370`;
- nodes: `461,992,054`;
- final AveragePolicy loss: `1.08852`.

TRUE_HEADS_UP:
- roots: `2,211,300`;
- Advantage seen: `66,712,536`;
- strategy seen: `2,217,546`;
- nodes: `324,876,215`;
- final AveragePolicy loss: `1.08346`.

Total roots: `4,860,000`.

The pilot wall time was about 3.60 hours.

## Fit stability during the 100 online iterations

Every HU iteration used all 8 members and all 273 HU roots.

Member final losses stayed in narrow, member-specific bands throughout the run. No NaN/nonfinite failure, root-count drift, or ensemble-size drift was observed in the report.

Aggregate sampled HU actions over the pilot were approximately:

- FOLD 16.1%;
- CHECK_CALL 29.0%;
- POT_33 13.4%;
- ALL_IN 39.2%;
- remaining active raise slots low-frequency.

Across successive 25-iteration blocks, ALL_IN stayed roughly 38.5–39.9%, so the training log itself shows no recurrence of the previous catastrophic one-model root-jam explosion.

This is only an execution/trajectory diagnostic, not a strategic EV pass.

## Artifact warning

The ordinary iteration-8100 checkpoint stores only the final single Advantage member in the bundle.

The complete current HU behavior is therefore:

**ordinary checkpoint + mandatory `hu_ensemble_state.pt` sidecar**.

The AveragePolicy inside the ordinary checkpoint is valid normally because it was trained from sampled trajectories generated by the ENS8 behavior.

## Next gate

Before any holdout:

1. reconstruct the exact pre-online ENS8_A behavior from the frozen 8000 reservoir;
2. compare deterministic root policy 8000 ENS8_A vs 8100 ENS8;
3. evaluate current ENS8 8100 on full forensic HU hands versus:
   - ENS8 8000,
   - production current 7600,
   - UNIFORM_LEGAL / PASSIVE_CALLER / JAMMER;
4. compare AveragePolicy 8100 vs AveragePolicy 8000 on the same baselines.

No new roots and no holdout.

Only if the online ENS8 behavior and its AveragePolicy survive this gate does trained-policy cross-play become the final design-set check before unsealing holdout.
